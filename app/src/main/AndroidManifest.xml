<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bankingsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankingSystem">
        <activity android:name=".ViewBillActivity"></activity>
        <activity android:name=".BalanceAdapter" />
        <activity android:name=".PayBillActivity" />
        <activity android:name=".TransferActivity" />
        <activity android:name=".WithdrawActivity" />
        <activity android:name=".DepositActivity" />
        <activity android:name=".CheckBalanceActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>